---
title: Keypairs
---

已实现：

- `ed25519`
- `secp256k1`（stdlib 兼容模式）
- `secp256r1`

```go
kp, err := ed25519.Generate()
if err != nil { panic(err) }

sig, err := kp.SignTransaction(txBytes)
if err != nil { panic(err) }

fmt.Println(kp.ToSuiAddress(), sig.Signature)
```
